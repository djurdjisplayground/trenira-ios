import SwiftUI

extension Color {
    /// Primary text — dark charcoal (#1C1C1E).
    static let slate850 = Color(red: 28 / 255, green: 28 / 255, blue: 30 / 255)

    /// Secondary labels (#6E6E73).
    static let slate500 = Color(red: 110 / 255, green: 110 / 255, blue: 115 / 255)

    /// Grouped list background, similar to Notes / Settings.
    static let groupedBackground = Color(red: 0.95, green: 0.95, blue: 0.97)

    /// Hairline separators and card outlines.
    static let hairline = Color(red: 0.82, green: 0.82, blue: 0.84)
}
